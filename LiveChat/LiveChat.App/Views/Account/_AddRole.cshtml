@model LiveChat.Domain.Models.EntityExtensions.RoleViewModel

<span class="btn btn-xs btn-success add-role" title="Remove role @Model.RoleName" 
    data-role-id="@Model.RoleId" data-user-id="@Model.UserId">
    <span>@Model.RoleName</span> 
    <span class="glyphicon glyphicon-plus"></span>
</span>